# Example environment file — SAFE to commit (contains no real secrets).
#
# In the Practical (実践編) session you will create a real ".env" file from
# this template, then add ".env" (NOT this file) to .gitignore so your
# secrets never reach GitHub.
#
# Copy this file:   cp .env.example .env
# Then edit ".env" with your (dummy, for training) values.

SECRET_API_KEY="training-dummy-key-12345"
